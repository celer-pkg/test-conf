{
    "url": "https://code.videolan.org/videolan/x264.git",
    "ref": "stable",
    "build_configs": [
        {
            "pattern": "*",
            "build_tool": "makefiles",
            "library_type": "shared",
            "patches": [
                "stable_fix_windows_build.patch"
            ],
            "env_vars": [
                "CPATH=/mingw64/include",
                "LIBRARY_PATH=/mingw64/lib"
            ],
            "options": [
                "--host=${HOST}",
                "--sysroot=${SYSROOT}",
                "--cross-prefix=${CROSS_PREFIX}",
                "--disable-cli",
                "--enable-pic"
            ],
            "dependencies": [ ],
            "cmake_config": "linux_shared"
        }
    ]
}